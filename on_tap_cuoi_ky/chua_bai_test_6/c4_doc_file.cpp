#include <iostream>
using namespace std;
void convert_case_and_reverse_string(int n) {

}



int main() {
    string path;
    cin >> path;
    ifstream file(path);
    int n;
    getline(file, n);
    while (!file.eof()) {
        string line;
        getline(file, line);
        if (file) {
            names.push_back(name);
        }
        else {
            cout << "Error while reading file occured";
            break;
        }
    }

    return 0;
}